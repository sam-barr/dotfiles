#!/bin/bash

set -e

git_pkgs=(xmonad-git xmonad-contrib-git mpv-git)

for pkg in "${git_pkgs[@]}"
do
    cd ~/.local/aur/"$pkg" || exit
    yes | makepkg -sricf
done

xmonad --recompile
xmonad --restart
